# ex3_1
עומק רקורסיה O(n)
ושימוש קבוע בזיכרון נוסף
סה"כ זיכרון: O(n).

# ex3_2
כל רמת רקורסיה נשמר sub בגודל (n-start) עד סיום הקריאה לרמת המשך, 
לכן סכום הגילויים: n + (n-1) + ... + 1 = O(n^2).
בנוסף עומק רקורסיה O(n) וקריאות רקורסיביות נוספות
סה"כ זיכרון: O(n^2).